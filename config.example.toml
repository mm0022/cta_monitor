[postgres]
host = "REPLACE"
port = "REPLACE"
user = "REPLACE"
password = "REPLACE"
database = "shiji"

[biyi]
login_url = "https://biyi.tky.laozi.pro/biyi/login"
base_url  = "https://biyi.tky.laozi.pro/biyi"
user = "REPLACE"
passwd = "REPLACE"

[datahub]
gateway_url = "https://nexus.tyo.cyberx.com/nexus-data-hub-gateway/"
api_key = "REPLACE"
prefix = "CYBERX_PROD"

[slack]
webhook_url = "REPLACE"    # incoming webhook URL（发文本表格，不发图片）

[monitor]
accounts = []              # 空 = 监控全部在跑 CTA 账户；否则填 biyi accountNames 白名单
min_notional_u = 10.0
freshness_hours = 1.0

[portfolio_accounts]
# 组合级发布器账户：account → portfolio_id（走 PORTFOLIO_PUBLISHER，一个 key 拆 per_token）；
# 不在此表的账户走 CTA_SIGNAL_PUBLISHER（每币一个 key）。留空表示无组合账户。
# binance_tokyo_cta_momentum_trade1 = "xs_carry_daily"
